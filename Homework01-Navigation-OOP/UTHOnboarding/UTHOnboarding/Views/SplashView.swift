import SwiftUI

struct SplashView: View {
    var body: some View {
        VStack{
            Spacer()
            Text("UTH SmartTask")
                .font(.largeTitle)
                .fontWeight(.bold)
            Spacer()
        }
    }
}
